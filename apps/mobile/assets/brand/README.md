# Ember (locked · Aura)

Same pack as `apps/web/public/brand/`. See `logo-directions.md`.

App icon / splash / adaptive icon use the cream Ember mark (`#F7F1EA` background). Chrome uses mark 24px + “Soft Spark”. Prefer PNG wordmark (`png/soft-spark-wordmark-840.png`) over the SVG wordmark.
