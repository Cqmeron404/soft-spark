{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git rev-parse --verify --quiet HEAD^ >/dev/null || exit 1; git diff --quiet HEAD^ HEAD -- apps/web packages/ui packages/shared package.json pnpm-lock.yaml"
}
